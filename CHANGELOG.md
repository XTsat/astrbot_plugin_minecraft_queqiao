# Changelog

## [Unreleased]

## v0.3.4 (2026-09-22)

### 新增
- **多服务器连接单群时按数字编号指定指令目标**：现在可在指令后加数字编号选择目标服务器（如`mc cmd 1 time set day` 定向第 1 台），解决「两台 MC 接入同一个群时无法把指令分到指定服务器」的问题；仅配置一台服务器时可直接省略服务器列表顺序一致
- **补充服务器列表配置信息**

## v0.3.3 (2026-09-22)

### 新增
- **`mc list` 免权限查询**：在线玩家列表改为三层降级机制
  1. 鹊桥 RCON
  2. 直连 RCON
  3. 在线人数（获取不到玩家时只显示在线人数）
- **`mc status` 显示在线玩家名**：状态查询顺带列出 SLP sample 的玩家名
- **配置列表条目标题显示服务器名称**

### 变更
- **`server_id` 配置提示补充**：`server_id` 的 hint 增加说明——配置多台服务器时
  每台的 `server_id` 必须互不相同，否则会导致连接冲突

## v0.3.2 (2026-09-22)

### 变更
- **统一显示名**：metadata、README 与代码各处统一为「Minecraft 鹊桥互通」
- **补全图片互通介绍**：desc 与 tags 补充图片互通（中英同步）
- **CHANGELOG 改分组式**：历史版本重组为 ### 新增/修复/变更 子标题分组
- **AGENTS.md 对齐上游模板**：第5节描述更新、目录结构补全 image_host/image_bed
- **requirements 注释同步**：同步配置项重命名（enable_image_http → 根级 enable_image_upload + image_upload_services）

## v0.3.1 (2026-09-22)

### 修复
- **直连 RCON 兜底**：配置无法显示与编辑问题

## v0.3.0 (2026-09-21)

### 新增
- **双向图片转发**：
  - 群消息中的图片自动上传到图床后转发到游戏内（游戏端需安装 ChatImage 模组渲染）
  - 游戏内聊天图片自动转发到外部会话：识别游戏内消息里的 ChatImage 代码或图片链接，自动下载后作为图片消息发送，下载失败保留原文
- **图片自动上传图床**：传递给游戏内
- **预置多种图床模板**
- **游戏内图片显示名称**：配置项
- **图床上传超时时间**：配置（默认 30 秒），上传超时视为该图床失败并自动切换下一个
- **内置图片 HTTP 服务模板**：该服务不上传第三方，但**要求 AstrBot 有公网可达地址**

## v0.2.2 (2026-09-21)

### 新增
- **多次重连失败进入低频重试**：连续重连失败超过 30 次后，间隔不再随失败次数递增，改为固定等待 300 秒，避免长期断线时高频打扰，两个阈值均可自定义

### 变更
- **重连配置独立分组**：重连相关配置从「鹊桥连接信息」分组移出，在服务器列表项最底部单独成「重连配置」分组

## v0.2.1 (2026-09-21)

### 新增
- **平台名称映射配置**：把原始平台名替换为自定义展示名，默认自带 `aiocqhttp=QQ` 示例

## v0.2.0 (2026-09-21)

### 新增
- **服务器显示名称**：配置项，用于展示服务器来源，为空时显示为 MC

### 变更
- **群→MC 转发前缀默认值**：由 `*` 改为留空，默认全部转发

### 修复
- **AI 回复重复两遍**：根因是**超时被误判为失败**——鹊桥 WS 发送成功但 10s 未收到响应，插件却判定私聊失败并自动改用广播重发，玩家遂收到两份回复
- **成就消息丢失成就名**：未开启翻译时成就消息丢失玩家名

## v0.1.1 (2026-09-20)

### 新增
- **转发成功汇报**：`emoji` 给原消息贴表情、`text` 回复 ✅ 文本、`none` 不提醒，贴表情失败不影响转发本身
- **自定义贴表情**：默认👌，推荐的 QQ 表情 ID：124=👌、76=👍、66=❤️、63=🌹

### 变更
- **默认配置开箱即用**：本机双端部署，连接信息无需修改，只需填写「目标会话」

## v0.1.0 (2026-09-20)

### 新增
- **基于鹊桥 V2 协议的 Minecraft 群服互通**：正向 / 反向 WebSocket 双模式
- **消息双向转发**：支持聊天格式自定义、转发前缀过滤与回声抑制
- **玩家事件转发**：加入 / 离开 / 死亡 / 成就事件（死亡与成就：原版端与 Velocity 不支持）
- **服务器状态查询与在线玩家列表**
- **远程指令执行**：鹊桥 RCON 优先、直连 RCON 兜底，支持指令黑白名单
- **账号与游戏 ID 绑定**：支持自定义指令占位符
- **游戏内 AI 聊天**：通过聊天前缀触发并私聊回复
- **AI 前缀与互通前缀语义互斥**：命中 AI 前缀的消息不再转发到群
- **前缀冲突检测**：启动时检测前缀配置相互包含的情况并输出告警
- **多服务器配置**
