{
  "enabled": {
    "type": "bool",
    "description": "启用 Minecraft 鹊桥互通",
    "default": true
  },
  "mc_servers": {
    "type": "template_list",
    "description": "MC服务器列表",
    "templates": {
      "server": {
        "name": "MC服务器",
        "display_item": "server.display_name",
        "hide_hint_in_list": true,
        "hint": "通过鹊桥连接的 MC 服务器配置",
        "items": {
          "enabled": {
            "type": "bool",
            "description": "启用此服务器",
            "default": true
          },
          "target_sessions": {
            "type": "list",
            "description": "目标会话",
            "hint": "MC服务器消息互通的目标会话UMO列表。可通过 AstrBot 自带 /sid 指令获取，格式如 aiocqhttp:GroupMessage:123456789。用于MC消息转发到外部、外部消息转发到MC以及区分指令所属服务器",
            "default": []
          },
          "server": {
            "description": "鹊桥连接信息",
            "type": "object",
            "items": {
              "server_name": {
                "type": "string",
                "description": "服务器名称",
                "hint": "用于区分不同服务器的唯一标识。正向模式下作为 x-self-name 发送，必须与鹊桥 config.yml 中的 server_name 一致。配置多台服务器时，每台的服务器名称必须互不相同，否则会导致连接冲突",
                "default": "Server"
              },
              "display_name": {
                "type": "string",
                "description": "服务器显示名称",
                "hint": "展示在消息格式与状态查询中的服务器名称，可写中文（例如『生存服』）。留空则使用下方「显示名称默认值」。仅用于展示，不影响连接",
                "default": ""
              },
              "display_name_default": {
                "type": "string",
                "description": "显示名称默认值",
                "hint": "服务器显示名称留空时，{display_name} 占位符与状态查询展示的内容，默认 MC（什么都不填即显示 [MC]）。改成留空则 {display_name} 输出空字符串（无前缀效果）",
                "default": "MC"
              },
              "ws_mode": {
                "type": "string",
                "description": "连接方式",
                "hint": "forward: 本插件主动连接鹊桥的 WebSocket Server；reverse: 本插件开启 WebSocket Server 等待鹊桥连入（租赁服推荐）",
                "options": [
                  "forward",
                  "reverse"
                ],
                "default": "forward"
              },
              "ws_url": {
                "type": "string",
                "description": "鹊桥 WebSocket 地址",
                "hint": "正向模式使用，需与鹊桥 config.yml 的 websocket_server 配置对应",
                "default": "ws://127.0.0.1:8080/minecraft/ws"
              },
              "reverse_host": {
                "type": "string",
                "description": "反向监听地址",
                "hint": "反向模式使用，留空或 0.0.0.0 表示监听所有网卡",
                "default": "0.0.0.0"
              },
              "reverse_port": {
                "type": "int",
                "description": "反向监听端口",
                "hint": "反向模式使用，需填入鹊桥 config.yml 的 websocket_client.url_list 中",
                "default": 8080
              },
              "reverse_path": {
                "type": "string",
                "description": "反向监听路径",
                "default": "/minecraft/ws"
              },
              "access_token": {
                "type": "string",
                "description": "认证 Token",
                "hint": "对应鹊桥 config.yml 中的 access_token，留空则免鉴权",
                "default": ""
              },
              "client_origin": {
                "type": "string",
                "description": "对接来源标识",
                "hint": "作为 x-client-origin 发送，用于避免同来源重复连接，非必要请勿修改",
                "default": "astrbot"
              }
            }
          },
          "enable_ai_chat": {
            "type": "bool",
            "description": "启用 AI 对话功能",
            "hint": "开启后，游戏内聊天以 AI 前缀开头时与 AI 对话，回复私聊给该玩家。不影响正常的群服互通",
            "default": true
          },
          "ai_chat_prefix": {
            "type": "string",
            "description": "AI 对话触发前缀",
            "hint": "游戏内聊天以此前缀开头时触发 AI（例如填写 ai 则发送『ai 你好』）。命中该前缀的消息只走 AI，不再转发到群。留空表示不启用 AI 触发，普通聊天不受影响",
            "default": "ai"
          },
          "text2image": {
            "type": "bool",
            "description": "将服务器信息渲染为图片输出",
            "hint": "渲染失败会自动回退为文本发送",
            "default": true
          },
          "message": {
            "description": "消息转发配置",
            "type": "object",
            "items": {
              "forward_chat_to_astrbot": {
                "type": "bool",
                "description": "转发聊天消息到指定会话",
                "hint": "将 Minecraft 玩家聊天消息转发到指定的外部会话",
                "default": true
              },
              "forward_chat_format": {
                "type": "string",
                "description": "聊天消息格式",
                "hint": "转发时的聊天消息格式。{player} 替换为玩家名称，{message} 替换为消息内容，{display_name} 替换为服务器显示名称（可中文，如 生存服；未填则用显示名称默认值，默认 MC）。示例：<{player}> {message}",
                "default": "[{display_name}]{player}: {message}"
              },
              "forward_join_leave_to_astrbot": {
                "type": "bool",
                "description": "转发玩家进出消息",
                "hint": "将玩家加入/离开服务器的消息转发到指定会话。多台服务器指向同一会话时，消息末尾会自动附上该服务器的显示名称（如「🟢 Alex 加入了服务器[生存服]」）以区分来源",
                "default": false
              },
              "forward_death_to_astrbot": {
                "type": "bool",
                "description": "转发玩家死亡消息",
                "hint": "原版端与 Velocity 不支持该事件",
                "default": false
              },
              "forward_achievement_to_astrbot": {
                "type": "bool",
                "description": "转发玩家成就消息",
                "hint": "原版端与 Velocity 不支持该事件",
                "default": false
              },
              "auto_forward_prefix": {
                "type": "string",
                "description": "外部消息转发到MC的前缀",
                "hint": "外部会话中以此前缀开头的消息才被转发到MC服务器（例如: *）；默认留空 = 全部转发（仅对已绑定目标会话的会话生效）。请勿与上面的 AI 前缀相互包含，否则同一条消息的归属会产生歧义",
                "default": ""
              },
              "broadcast_format": {
                "type": "string",
                "description": "外部消息转发到MC的格式",
                "hint": "发送到游戏内的格式。{platform} 平台名，{sender} 发送者名，{message} 消息内容，{display_name} 服务器显示名称（可中文；未填则用显示名称默认值，默认 MC），{server_name} 服务器名称（始终有值）",
                "default": "[{platform}]{sender}: {message}"
              },
              "platform_names": {
                "type": "list",
                "description": "平台名称映射",
                "hint": "把 {platform} 的原始平台名替换为自定义展示名，例如 aiocqhttp=QQ、telegram=电报。每项格式：原始平台名=显示名。默认自带示例 aiocqhttp=QQ（开箱即用）；未命中的平台保持原名，删空则不改写",
                "default": [
                  "aiocqhttp=QQ"
                ]
              },
              "broadcast_color": {
                "type": "string",
                "description": "转发消息颜色",
                "hint": "MC 颜色名（如 aqua、green、white）或 #RRGGBB 格式",
                "default": "white"
              },
              "mark_option": {
                "type": "string",
                "description": "转发提醒方式",
                "hint": "选择转发成功后的提醒方式：text为文本提醒，emoji为贴表情，none为不提醒",
                "options": [
                  "text",
                  "emoji",
                  "none"
                ],
                "default": "emoji"
              },
              "mark_emoji_id": {
                "type": "int",
                "description": "回执表情 ID",
                "hint": "转发成功后所贴的表情（仅 mark_option=emoji 且平台为 aiocqhttp 时生效）。填 QQ 表情 ID，默认 124 为 👌、76 为 👍、66 为 ❤️、63 为 🌹",
                "default": 124
              },
              "forward_image_to_mc": {
                "type": "bool",
                "description": "转发图片到游戏内",
                "hint": "外部会话消息中的图片自动转发到 MC 游戏内。需要游戏端安装 ChatImage 模组（https://github.com/kitUIN/ChatImage）才会渲染成图片，否则显示为 [[CICode,...]] 文本。图片以广播消息携带 URL 的形式发送，玩家客户端自行加载，需保证图片 URL 对玩家客户端可达",
                "default": false
              },
              "chatimage_name": {
                "type": "string",
                "description": "游戏内图片显示名称",
                "hint": "ChatImage 在聊天栏显示图片时使用的名称（CICode 的 name 参数），默认「图片」，可改成自己习惯的显示名",
                "default": "图片"
              },
              "forward_image_from_mc": {
                "type": "bool",
                "description": "转发游戏内图片到外部会话",
                "hint": "游戏内聊天消息中的图片（ChatImage 的 [[CICode,url=...]] 代码或直接贴出的图片链接）自动下载后作为图片发送到外部会话。默认关闭。下载失败（如 file:// 本地文件不在本机、链接失效）时保留原始文本（代码/链接），不静默丢失",
                "default": false
              }
            }
          },
          "cmd": {
            "description": "远程指令配置",
            "type": "object",
            "items": {
              "enabled": {
                "type": "bool",
                "description": "启用远程执行指令",
                "hint": "是否允许通过 /mc cmd 指令远程执行服务器命令，依赖鹊桥开启 RCON",
                "default": true
              },
              "cmd_white_black_list": {
                "type": "string",
                "description": "指令名单类型",
                "hint": "white: 仅允许名单内指令；black: 禁止名单内指令；none: 不启用黑白名单",
                "options": [
                  "white",
                  "black",
                  "none"
                ],
                "default": "white"
              },
              "cmd_list": {
                "type": "list",
                "description": "指令名单",
                "hint": "指令白名单或黑名单，填写指令名（不带/）如 say, gamemode, tp",
                "default": [
                  "say",
                  "list",
                  "weather",
                  "time"
                ]
              },
              "bind_enable": {
                "type": "bool",
                "description": "启用绑定功能",
                "hint": "允许用户将外部平台账号与MC游戏ID绑定",
                "default": true
              },
              "custom_cmd_list": {
                "type": "list",
                "description": "自定义指令映射",
                "hint": "自定义快捷指令。格式：触发词 <&参数&><<>>实际指令 {参数} {sender}。{sender}为用户绑定的游戏ID",
                "default": []
              },
              "rcon_enabled": {
                "type": "bool",
                "description": "启用直连 RCON 兜底",
                "hint": "当鹊桥未开启 RCON 时，改为由本插件直连服务器 RCON 端口执行指令",
                "default": false
              },
              "rcon_host": {
                "type": "string",
                "description": "RCON 地址",
                "hint": "直连 RCON 时服务器 RCON 监听的地址（需在服务端开启 RCON 并填密码）",
                "default": "localhost"
              },
              "rcon_port": {
                "type": "int",
                "description": "RCON 端口",
                "default": 25575
              },
              "rcon_password": {
                "type": "string",
                "description": "RCON 密码",
                "default": ""
              }
            }
          },
          "reconnect": {
            "description": "重连配置",
            "type": "object",
            "items": {
              "reconnect_interval": {
                "type": "int",
                "description": "重连间隔（秒）",
                "hint": "重连等待时间会随失败次数递增，上限 60 秒",
                "default": 5
              },
              "max_reconnect": {
                "type": "int",
                "description": "最大重连次数",
                "hint": "0 表示无限重连",
                "default": 0
              },
              "low_frequency_threshold": {
                "type": "int",
                "description": "进入低频重试的失败次数",
                "hint": "连续重连失败超过该次数后，重连间隔不再随失败次数递增，改为固定使用下方的低频重试间隔，避免长期断线时高频打扰。默认 30（即前 30 次正常退避，第 31 次起进入低频）；配 0 则关闭低频，始终按退避重连",
                "default": 30
              },
              "low_frequency_interval": {
                "type": "int",
                "description": "低频重试间隔（秒）",
                "hint": "进入低频重试后的固定等待时间，单位秒。默认 300（5 分钟）；可配更大值实现更安静的重试",
                "default": 300
              }
            }
          }
        }
      }
    }
  },
  "enable_image_upload": {
    "type": "bool",
    "description": "启用图片转存服务",
    "hint": "总开关：开启后使用下方 image_upload_services 列表里的条目，把无公开 URL 的图片转存换取玩家可访问的链接。条目支持两类：「内置图片HTTP服务」把图片转存到本机 HTTP 服务（不依赖第三方），第三方图床则上传到公网图床；两类可混排，内置优先。注意第三方图床会把群图片上传到外部服务",
    "default": false
  },
  "image_upload_timeout": {
    "type": "int",
    "description": "图床上传超时（秒）",
    "hint": "第三方图床上传接口单次请求的超时时间。超时视为该条目失败，自动切换列表里的下一个条目。默认 30 秒。注意：调太大如果图片卡住需要较久才能降级到下一个图床",
    "default": 30
  },
  "image_upload_services": {
    "type": "template_list",
    "description": "图片转存服务列表",
    "hint": "点「添加条目」选择服务类型（内置图片HTTP服务 / 第三方图床），多个服务按列表顺序逐个尝试，前一个失败会自动切换到下一个",
    "default": [],
    "templates": {
      "builtin_http": {
        "name": "内置图片HTTP服务",
        "hint": "本地：不上传第三方，需有公网地址",
        "items": {
          "enabled": {
            "type": "bool",
            "description": "启用此服务",
            "default": true
          },
          "name": {
            "type": "string",
            "description": "显示名称（可空）",
            "hint": "仅用于日志与界面辨识，留空则用默认名称「内置图片HTTP服务」",
            "default": "内置图片HTTP服务"
          },
          "host": {
            "type": "string",
            "description": "监听地址",
            "hint": "一般保持 0.0.0.0 监听所有网卡；若 AstrBot 与 MC 服务器同机且只给局域网玩家用，可填内网 IP",
            "default": "0.0.0.0"
          },
          "port": {
            "type": "int",
            "description": "监听端口",
            "hint": "玩家客户端需要能访问该端口（防火墙/安全组需放行）。默认 8765，与现有服务冲突时自行更换",
            "default": 8765
          },
          "base_url": {
            "type": "string",
            "description": "对外地址前缀（玩家可达）",
            "hint": "玩家客户端访问图片服务所用的地址，如 http://公网IP:8765 或 http://域名:8765。不能填 127.0.0.1（玩家客户端要能连到）。留空则服务不启动——即使本条目勾选了启用",
            "default": ""
          }
        }
      },
      "custom": {
        "name": "自定义图床",
        "hint": "通用：自由填写接口与响应格式",
        "items": {
          "enabled": {
            "type": "bool",
            "description": "启用此图床",
            "default": true
          },
          "name": {
            "type": "string",
            "description": "显示名称（可空）",
            "hint": "仅用于日志与界面辨识，留空则用上传接口地址的主机名",
            "default": ""
          },
          "upload_url": {
            "type": "string",
            "description": "上传接口地址（完整 URL）",
            "hint": "图床的上传接口，如自建兰空图床的 http://IP:7791/api/v1/upload。不限定服务商，填任意图床的上传地址即可。注意是接口地址，不是网站首页",
            "default": ""
          },
          "token": {
            "type": "string",
            "description": "鉴权 Token（可空）",
            "hint": "需要鉴权的图床填 API Token，会作为 Authorization: Bearer 请求头并附带表单字段 token 发送；免 token 的图床留空",
            "default": ""
          },
          "response": {
            "type": "string",
            "description": "响应解析方式",
            "hint": "text = 响应正文就是图片链接；json = 取 JSON 响应中键名为 url 的字段（无则取第一个形如 http(s):// 的字段）",
            "default": "text"
          },
          "file_field": {
            "type": "string",
            "description": "上传文件字段名",
            "hint": "multipart 表单里文件字段的名称；绝大多数图床是 file，个别是 image（如 img402.dev），插件会按此发送",
            "default": "file"
          },
          "headers": {
            "type": "string",
            "description": "自定义请求头（可空）",
            "hint": "需要额外请求头的图床填：一行一个「名字: 值」，如 Accept: application/json；也支持 JSON 对象格式（如 {\"X-Api-Key\":\"abc\"}）。token 字段会自动发送 Authorization: Bearer，无需在此重复",
            "default": ""
          },
          "form_fields": {
            "type": "string",
            "description": "额外表单字段（可空）",
            "hint": "少数图床除文件外还要求额外的普通表单字段时填：一行一个「名字: 值」，如 time: 72h；也支持 JSON 对象格式。字段名别与文件字段名/file 字段冲突",
            "default": ""
          }
        }
      },
      "catbox": {
        "name": "catbox",
        "hint": "海外：免token，单张≤200MB",
        "items": {
          "enabled": {
            "type": "bool",
            "description": "启用此图床",
            "default": true
          },
          "name": {
            "type": "string",
            "description": "显示名称（可空）",
            "hint": "仅用于日志与界面辨识，留空则用上传接口地址的主机名",
            "default": "catbox"
          },
          "upload_url": {
            "type": "string",
            "description": "上传接口地址（完整 URL）",
            "hint": "catbox.moe 上传接口（插件自动附带 reqtype=fileupload 兼容）",
            "default": "https://catbox.moe/user/api.php"
          },
          "token": {
            "type": "string",
            "description": "鉴权 Token（可空）",
            "hint": "需要鉴权的图床填 API Token，会作为 Authorization: Bearer 请求头并附带表单字段 token 发送；免 token 的图床留空",
            "default": ""
          },
          "response": {
            "type": "string",
            "description": "响应解析方式",
            "hint": "text = 响应正文就是图片链接；json = 取 JSON 响应中键名为 url 的字段（无则取第一个形如 http(s):// 的字段）",
            "default": "text"
          },
          "file_field": {
            "type": "string",
            "description": "上传文件字段名",
            "hint": "multipart 表单里文件字段的名称；绝大多数图床是 file，个别是 image（如 img402.dev），插件会按此发送",
            "default": "fileToUpload"
          }
        }
      },
      "litterbox": {
        "name": "litterbox",
        "hint": "海外：免token，临时存储≤1GB",
        "items": {
          "enabled": {
            "type": "bool",
            "description": "启用此图床",
            "default": true
          },
          "name": {
            "type": "string",
            "description": "显示名称（可空）",
            "hint": "仅用于日志与界面辨识，留空则用上传接口地址的主机名",
            "default": "litterbox"
          },
          "upload_url": {
            "type": "string",
            "description": "上传接口地址（完整 URL）",
            "hint": "litterbox 上传接口（catbox 的临时分支，插件自动附带 reqtype=fileupload 兼容）",
            "default": "https://litterbox.catbox.moe/resources/internals/api.php"
          },
          "token": {
            "type": "string",
            "description": "鉴权 Token（可空）",
            "hint": "需要鉴权的图床填 API Token，会作为 Authorization: Bearer 请求头并附带表单字段 token 发送；免 token 的图床留空",
            "default": ""
          },
          "response": {
            "type": "string",
            "description": "响应解析方式",
            "hint": "text = 响应正文就是图片链接；json = 取 JSON 响应中键名为 url 的字段（无则取第一个形如 http(s):// 的字段）",
            "default": "text"
          },
          "file_field": {
            "type": "string",
            "description": "上传文件字段名",
            "hint": "multipart 表单里文件字段的名称；绝大多数图床是 file，个别是 image（如 img402.dev），插件会按此发送",
            "default": "fileToUpload"
          },
          "form_fields": {
            "type": "string",
            "description": "额外表单字段（可空）",
            "hint": "litterbox 必填 time 指定保留时长，可选 1h/12h/24h/72h，到期自动删除；漏填服务端返回 500。一行一个「名字: 值」，也支持 JSON 对象格式",
            "default": "time: 72h"
          }
        }
      },
      "imglink": {
        "name": "imglink",
        "hint": "海外：免token，单张≤25MB",
        "items": {
          "enabled": {
            "type": "bool",
            "description": "启用此图床",
            "default": true
          },
          "name": {
            "type": "string",
            "description": "显示名称（可空）",
            "hint": "仅用于日志与界面辨识，留空则用上传接口地址的主机名",
            "default": "imglink"
          },
          "upload_url": {
            "type": "string",
            "description": "上传接口地址（完整 URL）",
            "hint": "imglink.cc 上传接口；服务端会重新编码图片，不适合原样存档",
            "default": "https://imglink.cc/api/upload"
          },
          "token": {
            "type": "string",
            "description": "鉴权 Token（可空）",
            "hint": "需要鉴权的图床填 API Token，会作为 Authorization: Bearer 请求头并附带表单字段 token 发送；免 token 的图床留空",
            "default": ""
          },
          "response": {
            "type": "string",
            "description": "响应解析方式",
            "hint": "text = 响应正文就是图片链接；json = 取 JSON 响应中键名为 url 的字段（无则取第一个形如 http(s):// 的字段）",
            "default": "json"
          }
        }
      },
      "img402": {
        "name": "img402.dev",
        "hint": "海外：免token，单张≤10MB",
        "items": {
          "enabled": {
            "type": "bool",
            "description": "启用此图床",
            "default": true
          },
          "name": {
            "type": "string",
            "description": "显示名称（可空）",
            "hint": "仅用于日志与界面辨识，留空则用上传接口地址的主机名",
            "default": "img402"
          },
          "upload_url": {
            "type": "string",
            "description": "上传接口地址（完整 URL）",
            "hint": "img402.dev 免费上传接口（文件字段名是 image，非 file）",
            "default": "https://img402.dev/api/free"
          },
          "token": {
            "type": "string",
            "description": "鉴权 Token（可空）",
            "hint": "需要鉴权的图床填 API Token，会作为 Authorization: Bearer 请求头并附带表单字段 token 发送；免 token 的图床留空",
            "default": ""
          },
          "response": {
            "type": "string",
            "description": "响应解析方式",
            "hint": "text = 响应正文就是图片链接；json = 取 JSON 响应中键名为 url 的字段（无则取第一个形如 http(s):// 的字段）",
            "default": "json"
          },
          "file_field": {
            "type": "string",
            "description": "上传文件字段名",
            "hint": "multipart 表单里文件字段的名称；绝大多数图床是 file，个别是 image（如 img402.dev），插件会按此发送",
            "default": "image"
          }
        }
      },
      "pngurl": {
        "name": "pngurl",
        "hint": "国内：访客免token",
        "items": {
          "enabled": {
            "type": "bool",
            "description": "启用此图床",
            "default": true
          },
          "name": {
            "type": "string",
            "description": "显示名称（可空）",
            "hint": "仅用于日志与界面辨识，留空则用上传接口地址的主机名",
            "default": "pngurl"
          },
          "upload_url": {
            "type": "string",
            "description": "上传接口地址（完整 URL）",
            "hint": "pngurl.com 上传接口（原 pngcdn.cn 已停用，接口根地址为 /api/v1）",
            "default": "https://pngurl.com/api/v1/upload"
          },
          "token": {
            "type": "string",
            "description": "鉴权 Token（可空）",
            "hint": "pngurl.com 用户在 https://pngurl.com/user/tokens 创建永久 token；访客上传开启时可留空。会作为 Authorization: Bearer 请求头并附带表单字段 token 发送",
            "default": ""
          },
          "response": {
            "type": "string",
            "description": "响应解析方式",
            "hint": "text = 响应正文就是图片链接；json = 取 JSON 响应中键名为 url 的字段（无则取第一个形如 http(s):// 的字段）",
            "default": "json"
          },
          "headers": {
            "type": "string",
            "description": "自定义请求头（可空）",
            "hint": "pngurl.com 需要携带 Accept: application/json。一行一个「名字: 值」，也支持 JSON 对象格式（如 {\"X-Api-Key\":\"abc\"}）；token 字段会自动发送 Authorization: Bearer，无需在此重复",
            "default": "Accept: application/json"
          }
        }
      },
      "picui": {
        "name": "picui",
        "hint": "国内：免token",
        "items": {
          "enabled": {
            "type": "bool",
            "description": "启用此图床",
            "default": true
          },
          "name": {
            "type": "string",
            "description": "显示名称（可空）",
            "hint": "仅用于日志与界面辨识，留空则用上传接口地址的主机名",
            "default": "picui"
          },
          "upload_url": {
            "type": "string",
            "description": "上传接口地址（完整 URL）",
            "hint": "picui.cn 上传接口（Lsky 风格，游客可免 token 上传）",
            "default": "https://picui.cn/api/v1/upload"
          },
          "token": {
            "type": "string",
            "description": "鉴权 Token（可空）",
            "hint": "pngurl.com 用户在 https://pngurl.com/user/tokens 创建永久 token；访客上传开启时可留空。会作为 Authorization: Bearer 请求头并附带表单字段 token 发送",
            "default": ""
          },
          "response": {
            "type": "string",
            "description": "响应解析方式",
            "hint": "text = 响应正文就是图片链接；json = 取 JSON 响应中键名为 url 的字段（无则取第一个形如 http(s):// 的字段）",
            "default": "json"
          },
          "headers": {
            "type": "string",
            "description": "自定义请求头（可空）",
            "hint": "picui.cn 需要携带 Accept: application/json。一行一个「名字: 值」，也支持 JSON 对象格式；token 字段会自动发送 Authorization: Bearer，无需在此重复",
            "default": "Accept: application/json"
          }
        }
      },
      "anyapi": {
        "name": "anyapi",
        "hint": "国内（中转）：免token，≤10MB",
        "items": {
          "enabled": {
            "type": "bool",
            "description": "启用此图床",
            "default": true
          },
          "name": {
            "type": "string",
            "description": "显示名称（可空）",
            "hint": "仅用于日志与界面辨识，留空则用上传接口地址的主机名",
            "default": "anyapi"
          },
          "upload_url": {
            "type": "string",
            "description": "上传接口地址（完整 URL）",
            "hint": "anyapi 自建图床接口（imageproxy 中转代理，免 token；建议单文件 ≤10MB）",
            "default": "https://imageproxy.zhongzhuan.chat/api/upload"
          },
          "token": {
            "type": "string",
            "description": "鉴权 Token（可空）",
            "hint": "需要鉴权的图床填 API Token，会作为 Authorization: Bearer 请求头并附带表单字段 token 发送；免 token 的图床留空",
            "default": ""
          },
          "response": {
            "type": "string",
            "description": "响应解析方式",
            "hint": "text = 响应正文就是图片链接；json = 取 JSON 响应中键名为 url 的字段（无则取第一个形如 http(s):// 的字段）",
            "default": "json"
          }
        }
      },
      "xinyew": {
        "name": "xinyew（360图床）",
        "hint": "国内：免token",
        "items": {
          "enabled": {
            "type": "bool",
            "description": "启用此图床",
            "default": true
          },
          "name": {
            "type": "string",
            "description": "显示名称（可空）",
            "hint": "仅用于日志与界面辨识，留空则用上传接口地址的主机名",
            "default": "xinyew"
          },
          "upload_url": {
            "type": "string",
            "description": "上传接口地址（完整 URL）",
            "hint": "360 图床（新野 API）接口，免 token；过小的图片可能被上游拒绝",
            "default": "https://api.xinyew.cn/api/360tc"
          },
          "token": {
            "type": "string",
            "description": "鉴权 Token（可空）",
            "hint": "需要鉴权的图床填 API Token，会作为 Authorization: Bearer 请求头并附带表单字段 token 发送；免 token 的图床留空",
            "default": ""
          },
          "response": {
            "type": "string",
            "description": "响应解析方式",
            "hint": "text = 响应正文就是图片链接；json = 取 JSON 响应中键名为 url 的字段（无则取第一个形如 http(s):// 的字段）",
            "default": "json"
          }
        }
      },
      "xunjinlu": {
        "name": "xunjinlu",
        "hint": "国内：免token，≤20MB",
        "items": {
          "enabled": {
            "type": "bool",
            "description": "启用此图床",
            "default": true
          },
          "name": {
            "type": "string",
            "description": "显示名称（可空）",
            "hint": "仅用于日志与界面辨识，留空则用上传接口地址的主机名",
            "default": "xunjinlu"
          },
          "upload_url": {
            "type": "string",
            "description": "上传接口地址（完整 URL）",
            "hint": "多接口自适应图床（自动按文件大小选上游，免 token；≤5MB 走随机接口，>20MB 拒绝）",
            "default": "https://api.xunjinlu.fun/tc/api.php"
          },
          "token": {
            "type": "string",
            "description": "鉴权 Token（可空）",
            "hint": "需要鉴权的图床填 API Token，会作为 Authorization: Bearer 请求头并附带表单字段 token 发送；免 token 的图床留空",
            "default": ""
          },
          "response": {
            "type": "string",
            "description": "响应解析方式",
            "hint": "text = 响应正文就是图片链接；json = 取 JSON 响应中键名为 url 的字段（无则取第一个形如 http(s):// 的字段）",
            "default": "json"
          }
        }
      },
      "imgloc": {
        "name": "imgloc",
        "hint": "国内：需token，单张约6MB",
        "items": {
          "enabled": {
            "type": "bool",
            "description": "启用此图床",
            "default": true
          },
          "name": {
            "type": "string",
            "description": "显示名称（可空）",
            "hint": "仅用于日志与界面辨识，留空则用上传接口地址的主机名",
            "default": "imgloc"
          },
          "upload_url": {
            "type": "string",
            "description": "上传接口地址（完整 URL）",
            "hint": "路过图床上传接口（需配套填 token）",
            "default": "https://api.imgse.com/1/upload"
          },
          "token": {
            "type": "string",
            "description": "鉴权 Token（可空）",
            "hint": "需要鉴权的图床填 API Token，会作为 Authorization: Bearer 请求头并附带表单字段 token 发送；免 token 的图床留空",
            "default": ""
          },
          "response": {
            "type": "string",
            "description": "响应解析方式",
            "hint": "text = 响应正文就是图片链接；json = 取 JSON 响应中键名为 url 的字段（无则取第一个形如 http(s):// 的字段）",
            "default": "json"
          }
        }
      },
      "see": {
        "name": "see（sm.ms）",
        "hint": "国内：需token，单张≤5MB",
        "items": {
          "enabled": {
            "type": "bool",
            "description": "启用此图床",
            "default": true
          },
          "name": {
            "type": "string",
            "description": "显示名称（可空）",
            "hint": "仅用于日志与界面辨识，留空则用上传接口地址的主机名",
            "default": "see"
          },
          "upload_url": {
            "type": "string",
            "description": "上传接口地址（完整 URL）",
            "hint": "S.EE（原 sm.ms）上传接口；sm.ms 旧接口已 308 迁移，需在此端点上传",
            "default": "https://s.ee/api/v1/file/upload"
          },
          "response": {
            "type": "string",
            "description": "响应解析方式",
            "hint": "text = 响应正文就是图片链接；json = 取 JSON 响应中键名为 url 的字段（无则取第一个形如 http(s):// 的字段）",
            "default": "json"
          },
          "file_field": {
            "type": "string",
            "description": "上传文件字段名",
            "hint": "S.EE 文件字段名是 smfile，非 file",
            "default": "smfile"
          },
          "headers": {
            "type": "string",
            "description": "自定义请求头（可空）",
            "hint": "S.EE 用 Authorization 直传 key（不带 Bearer 前缀），格式：Authorization: 你的key。在 s.ee 用户页 → 工具 → API 密钥令牌 获取",
            "default": "Authorization: "
          }
        }
      },
      "imgbb": {
        "name": "imgbb",
        "hint": "海外：需token，单张≤32MB",
        "items": {
          "enabled": {
            "type": "bool",
            "description": "启用此图床",
            "default": true
          },
          "name": {
            "type": "string",
            "description": "显示名称（可空）",
            "hint": "仅用于日志与界面辨识，留空则用上传接口地址的主机名",
            "default": "imgbb"
          },
          "upload_url": {
            "type": "string",
            "description": "上传接口地址（完整 URL）",
            "hint": "imgbb 上传接口，key 在 https://api.imgbb.com/ 免费注册",
            "default": "https://api.imgbb.com/1/upload"
          },
          "token": {
            "type": "string",
            "description": "鉴权 Token（可空）",
            "hint": "imgbb API key，https://api.imgbb.com/ 免费注册。会作为 Authorization: Bearer 请求头并附带表单字段 token 发送",
            "default": ""
          },
          "response": {
            "type": "string",
            "description": "响应解析方式",
            "hint": "text = 响应正文就是图片链接；json = 取 JSON 响应中键名为 url 的字段（无则取第一个形如 http(s):// 的字段）",
            "default": "json"
          },
          "file_field": {
            "type": "string",
            "description": "上传文件字段名",
            "hint": "imgbb 文件字段名是 image，非 file",
            "default": "image"
          }
        }
      }
    }
  }
}
